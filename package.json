{
  "scripts": {
    "dev": "next dev",
    "build": "next build && (test -d out || (echo \"ERROR: out/ not found. Export step didn't run.\" && exit 1)) && rm -rf docs && rsync -a --delete out/ docs/ && touch docs/.nojekyll",
    "start": "next start",
    "lint": "eslint"
  }
}
